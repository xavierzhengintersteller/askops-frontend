import { PageContainer } from '@ant-design/pro-components';
import { Typography } from 'antd';

export default function AdminGroup() {
  return (
    <PageContainer title="分组管理">
      <Typography.Text>分组管理界面（可自行扩展）</Typography.Text>
    </PageContainer>
  );
}
